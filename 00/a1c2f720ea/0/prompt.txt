PR merged and branch deleted. batching for Plan 4 is confirmed, go ahead

---

<task-notification>
<task-id>ab94f458e8f2a5eb0</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/ab94f458e8f2a5eb0.output</output-file>
<status>completed</status>
<summary>Agent "Recon: baddg, stabddg" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>/home/briney/git/autobio/.superpowers/sdd/recon/scoring.md

Summary of wrinkles:
- Both tools currently share the fully generic `ScoringInput`/`ScoringOutput`; each needs a per-tool input subclass promoting its `extra` keys to typed fields. `baddg` needs 5 (`mutations: list[str]`, `chains: str`, `n_folds: int=3`, `seed: int=0`, `device: str="auto"`); `stabddg` needs 8 (`mutations`, `chains`, `mc_samples: int=20`, `noise_level: float=0.1`, `batch_size: int=10000`, `trials: int=1`, `seed: int=0`, `device: str="auto"`). Both also inherit an unused `sequences: dict[str,str]|None` field from `ScoringInput` that neither runner ever reads.
- Both have exactly one structure-path input (`structure_path: Path`, PDB), copied to `workspace.inputs_dir` and referenced in config as `pdb_path`; no other file/sequence inputs.
- `_validate_inputs` for `mutations` (non-empty list[str]) and `chains` (exactly one `_`) is near-identical copy-pasted code between the two runners — a shared validator/mixin would deduplicate it; these checks are also duplicated in each container's `validate_config.sh`.
- Output schema (`ScoringOutput`/`ScoredStructure`) is fully generic and adequate as-is for both — no new output fields needed; `structure_path` is always hardcoded `None`, `ddg`==`total_score`, and `score_breakdown` content differs only cosmetically (`fold_values`/`n_folds` for baddg vs `trial_values`/`n_trials` for stabddg).
- Gaps worth encoding as Pydantic constraints during migration: baddg's `n_folds` has a container-enforced 1–3 range not checked host-side; stabddg's `mc_samples` must be a positive int (container-enforced only); stabddg has zero host-side validation on `mc_samples`/`noise_level`/`batch_size`/`trials`/`seed`/`device`.
- `config["chains"] = input_data.extra["chains"]` uses direct dict indexing (not `.get`) in both runners, relying on validation having already run — fragile pattern that a typed field would eliminate entirely.</result>
<usage><subagent_tokens>104364</subagent_tokens><tool_uses>22</tool_uses><duration_ms>129870</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a0ec4f72dd6e6332b</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/a0ec4f72dd6e6332b.output</output-file>
<status>completed</status>
<summary>Agent "Recon: antipasti, prodigy" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>The fact sheet is written to `/home/briney/git/autobio/.superpowers/sdd/recon/affinity.md`.

Summary of wrinkles:

- No `ToolCategory` mismatch — both `antipasti` and `prodigy` use `ToolCategory.SCORING`, which is a valid enum member.
- `prodigy`'s `chain_selection` input field is renamed to `"selection"` in config.json — a silent field-name remap that must be preserved exactly.
- `prodigy`'s `score_breakdown` output shape is conditional: `contact_list` only appears in the dict when the raw tool output included it (driven by the `contact_list` extra flag) — not a uniform schema.
- `antipasti`'s `modes` extra key is `str | int` (default `"all"`, or an integer mode count) — not a plain single-type scalar.
- `antipasti`'s `checkpoint_path` is a hardcoded container-baked constant with no corresponding input field or `extra` key today.
- Both tools' `structure_path` fields are file/path candidates (PDB/mmCIF); `antigen_chains` is a `list[str]` (not scalar); neither schema has any `SequenceSet`-candidate fields (chain IDs, not sequences) or existing `x-autobio` UI hints.</result>
<usage><subagent_tokens>103877</subagent_tokens><tool_uses>28</tool_uses><duration_ms>155272</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>abf5fd210a24c3bf8</task-id>
<tool-use-id>toolu_01KWfoCTNZTHrZEmrYJBjNjx</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/abf5fd210a24c3bf8.output</output-file>
<status>completed</status>
<summary>Agent "Recon: boltz1/boltz2, rfd3" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>The fact sheet is written to `/home/briney/git/autobio/.superpowers/sdd/recon/sp-b.md`.

Summary of wrinkles:

- **boltz1 vs boltz2**: They are near-identical today — same runner, same image_tag (`boltz:1.0.0`), same shared `StructurePredictionInput`/`StructurePredictionOutput` (no per-tool subclass exists, unlike ESM's `ESM2Input`), same `config.json` shape and same `_CONSUMED_EXTRA_KEYS`. The *only* wired distinguishing signal is `self.tool_name` feeding a trivial identity dict (`_MODEL_CONFIG = {"boltz1": "boltz1", "boltz2": "boltz2"}`) that sets `config["model"]`. Registry-level differences are just `default_timeout` (3600 vs 7200), `description`, and `notes` (boltz2 documents 3 affinity-only `extra` keys: `sampling_steps_affinity`, `diffusion_samples_affinity`, `method`) — good candidates for a boltz2-only input subclass, mirroring how `ESM2Input` adds `checkpoint`.
- **rfd3 is mechanically harder than ESM's single-mode pattern** for two reasons: (1) it has **no `_CONSUMED_EXTRA_KEYS`/`_apply_extra` mechanism at all** — `extra` is flat-merged unconditionally (`config.update(input_data.extra)`), so migration must introduce the shadow-rejection pattern from scratch; (2) `design_specs: dict[str, dict[str, Any]]` is a large, genuinely untyped, tool-native mini-language (contig strings, `select_*` fields that accept bool|str|dict, symmetry, ligand codes, etc.) whose real validation lives in the container's bash `validate_config.sh`, not Python — it resists simple typed-field promotion and will likely need to remain an escape-hatch dict.
- Both `StructurePredictionInput` (boltz) and `StructureDesignInput` (rfd3) currently have **zero `ui()`/`x-autobio` hints**, unlike ESM's schemas — every field needs hints added from scratch.
- `DesignedStructure.evaluation_metrics` has a dead/aspirational docstring referencing a `mode='generate'` concept that doesn't exist anywhere in `RFD3Runner` — flagged as a design question (single-mode vs. anticipating a future design+evaluate pipeline mode).
- Boltz's `sequences: dict[str, str]` is a `SequenceSet` candidate but values can be DNA/RNA/SMILES/empty placeholders (not pure protein), so a naive FASTA-based `GenericSequenceSet` normalizer may not fit cleanly — flagged for design review rather than assumed.</result>
<usage><subagent_tokens>117268</subagent_tokens><tool_uses>27</tool_uses><duration_ms>216779</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ae0e1c3b3f070a2b9</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/ae0e1c3b3f070a2b9.output</output-file>
<status>completed</status>
<summary>Agent "Recon: esmfold, chai1, openfold3" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>The fact sheet has been written to `/home/briney/git/autobio/.superpowers/sdd/recon/sp-a.md`.

Summary of notable wrinkles:

- **Templates/MSA are host-validated + copied but never wired into config**: chai1 copies template files into `inputs/` and validates their existence, but no config key ever points to them; openfold3 has the same gap for both `templates` and `msa_paths` (chai1's `msa_directory` is at least wired via a config key; openfold3's `msa_paths` is not wired at all).
- **Raw-override None-value footguns**: chai1's `chai_fasta` uses presence-check in validation but truthiness-check in `prepare_workspace` (empty-`sequences` + `chai_fasta=None` slips through to produce a near-empty FASTA); openfold3's `query_json` is presence-checked consistently but a `None` value gets `json.dumps`'d into the literal string `"null"`, silently discarding any valid `sequences` also provided — a real correctness gap, not just style.
- **`parse_output` inconsistency**: esmfold requires `data["confidence"]` (KeyError if absent) while chai1/openfold3 default to `data.get("confidence", {})`.
- **Extra-key clobber risk**: chai1's/openfold3's `num_diffn_samples`/`num_diffusion_samples` (derived from `num_models`) aren't in `_CONSUMED_EXTRA_KEYS`, so a caller could override them via `extra`.
- **Chain ordering asymmetry**: chai1 sorts chain IDs alphabetically when building its FASTA; openfold3 preserves dict insertion order when building its query JSON.
- **Extra-key surface area**: esmfold has no `_CONSUMED_EXTRA_KEYS` at all (0 documented extra keys, full flat-merge); chai1 has 4 consumed + 7 documented pass-through keys; openfold3 has 4 consumed + 8 documented pass-through keys — chai1/openfold3 are meaningfully harder to migrate than the plain esm single-mode pattern given the structural (non-scalar) extras (`entity_types`, `constraints`/`query_json`, `msa_directory`/`msa_paths`, `non_canonical_residues`).
- **Shared boilerplate**: `_resolve_container_path` is copy-pasted verbatim in all three runners — a dedup candidate. Output schema itself shows no variance across the three (all share `StructurePredictionOutput`, and none populate the `affinity_*` fields on `PredictedStructure`).</result>
<usage><subagent_tokens>152549</subagent_tokens><tool_uses>28</tool_uses><duration_ms>341791</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ab5c8d4c94a7e9c82</task-id>
<tool-use-id>toolu_01AZRBjUyJ7D1dZxRSzUUd1T</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/ab5c8d4c94a7e9c82.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 1: strengthen _apply_extra" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE
Commit: `414ae67` — "tools: _apply_extra also rejects extra keys colliding with derived config keys"
Tests: 32/32 targeted tests pass (RED confirmed pre-fix); full suite `python -m pytest -m "not docker and not gpu"` → 1326 passed, 121 deselected; ruff/mypy clean.
Concerns: none — change is strictly additive (superset of prior rejections), only the 4 specified files touched, freesasa/esm passthrough tests stayed green.
Report: `/home/briney/git/autobio/.superpowers/sdd/task-1-report.md`</result>
<usage><subagent_tokens>60250</subagent_tokens><tool_uses>22</tool_uses><duration_ms>130882</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a14843fc288641226</task-id>
<tool-use-id>toolu_01GGMboUn4QprKtMEUsDKWTe</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/a14843fc288641226.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 1 (spec + quality)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All checks confirmed against the diff and source. Findings below.

### Spec Compliance
- ✅

The collision set is computed exactly as `typed_fields ∪ config_keys`: `typed_fields = set(self.current_mode.input_schema.model_fields) - {"extra"}` then `collisions = sorted(key for key in input_data.extra if key in typed_fields or key in config)` (`src/autobio/tools/base.py:224-225`). The `- {"extra"}` exclusion applies only to the typed-field set; `config` is checked directly as the live dict argument — matches the brief's snippet verbatim. Error message text concatenates to `"...collide with typed input fields or runner-derived config keys: ..."` (`base.py:228-230`), containing the required substring exactly. Docstring updated to describe both rejection reasons (`base.py:200-222`).

New test `test_apply_extra_rejects_derived_config_key_collision` (`tests/unit/test_tool_runner_modes.py:136-142`) passes `output_dir` in `config` while `_TypedInput` (line 35-36) declares only `alpha_param` (plus inherited `extra` from `BaseInput`, which has no other fields — confirmed in `src/autobio/schemas/base.py:27-36`). So `output_dir` is provably not a typed field, isolating the config-key clause. The pre-existing `test_apply_extra_rejects_shadowing_typed_field` (line 128-133) uses `config={}`, isolating the typed-field clause. Together the two tests independently exercise both halves of the union — exactly what the named risk asked for.

freesasa/esm passthrough tests are untouched: `test_extra_unknown_key_passed_through` (`tests/unit/test_freesasa.py:127-134`) passes `custom_flag`, which is neither a typed field nor a key freesasa's `prepare_workspace` writes into `config` (`mode`, `structure_path`, `algorithm`, `probe_radius`, `per_residue`, `output_dir`, optionally `partner1`/`partner2` — verified in `src/autobio/tools/freesasa.py`), so it still merges cleanly. (esm's test file has no equivalent genuinely-new-key passthrough test — that predates this task and is out of scope; not a regression.) Only the two shadow-test `match=` strings were updated in `test_freesasa.py:119` and `test_esm.py:195`, as the brief required.

Scope was respected: diff touches exactly the four permitted files, no more, no less.

### Strengths
- Collision logic is a faithful, minimal transcription of the brief's exact snippet — no creative deviation.
- Test design cleanly separates the two collision clauses (empty-config test for typed-field clause, non-typed-field-key test for config-key clause), which directly substantiates the named risk rather than leaving it to inference.
- Docstring calls out the call-order invariant (`_apply_extra` must run after all derived keys are written) — useful guardrail for the Task 2-5 migrations that inherit this helper.
- Verified independently: `ruff check` on the four touched files passes clean; line 225 (the `collisions = sorted(...)` comprehension) is exactly 100 chars, at but not over the configured `line-length = 100` limit.
- No stray references to the old "shadow typed input fields" wording remain in any test file (only in historical plan docs under `docs/superpowers/plans/`, which are point-in-time planning artifacts, not living docs — not a defect).

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `base.py:225` — the `collisions = sorted(...)` comprehension sits right at the 100-char line-length boundary, which is dense for a two-condition predicate. Splitting the generator across two lines (or naming the config/typed membership checks separately) would read slightly easier, but this is purely stylistic and ruff passes as-is.

### Assessment
**Task quality:** Approved
**Reasoning:** The collision computation, error message, docstring, and tests match the brief precisely; the named risk (union correctness, and the new test proving the config-key clause independent of the typed-field clause) is verifiably satisfied by reading the actual fixtures, and pre-existing passthrough tests are untouched and remain valid.</result>
<usage><subagent_tokens>66195</subagent_tokens><tool_uses>14</tool_uses><duration_ms>133095</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>afa2d03902c2105c2</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/afa2d03902c2105c2.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 2: migrate baddg" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: Done.
Commit: `6ff830f2d577ff589fbb5210f6b1350a116f1e76` — "baddg: migrate to catalog Tool with typed fields (predict mode)"
Tests: `test_baddg.py` + `test_baddg_e2e.py` + `test_registry_disjoint.py` → 43 passed; full fast suite (`-m "not docker and not gpu"`) → 1326 passed, 121 deselected; mypy clean; ruff clean.
Concerns: preserved `AutobioError` messages still say "in the extra dict" for mutations/chains, even though they're now typed fields (intentional, per brief's exact-message requirement — same pattern will recur in stabddg/Task 3; worth a follow-up copy pass across Tasks 2–5, not fixed here).
Report: `/home/briney/git/autobio/.superpowers/sdd/task-2-report.md`</result>
<usage><subagent_tokens>136732</subagent_tokens><tool_uses>54</tool_uses><duration_ms>377994</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>af027812ebf4e569b</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/af027812ebf4e569b.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 2 (baddg migration)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Now I have everything needed for the review.

### Spec Compliance
- ✅ Byte-compat `config.json` contract: `src/autobio/tools/baddg.py:63-79` — key order and values match the brief exactly (`pdb_path`, `mutations=",".join(input_data.mutations)`, `chains`, `mpnn_checkpoint_path`, `ddg_checkpoint_path`, `output_dir`, `n_folds`, `seed`, `device`, then `self._apply_extra(config, input_data)`). Test coverage spans `test_basic_config` (6 keys) + `test_default_params` (n_folds/seed/device) — together the full 9-key contract is pinned, just split across two tests rather than one dict-equality assert.
- ✅ `BAddGInput(BaseInput)` — `src/autobio/schemas/scoring.py:71` — does not inherit `ScoringInput`/dead `sequences` field; `ScoringInput` itself untouched by the diff.
- ✅ Flat registry removed / catalog registered — `TOOL_REGISTRY["baddg"]` and `_CONSUMED_EXTRA_KEYS` deleted; `BADDG_TOOL` + `register(BADDG_TOOL)` present at `src/autobio/tools/baddg.py:160-188`; `tests/unit/test_registry_disjoint.py` guard applies and (per report) passes; `TOOL_RUNNERS["baddg"]` in `src/autobio/tools/__init__.py:72` untouched, still `BAddGRunner`.
- ✅ (assessed on merits, not a defect) `AutobioError` messages still reading "in the extra dict" (`src/autobio/tools/baddg.py:120,127`) — the brief explicitly mandates "same messages" verbatim for `_validate_inputs`, so this is spec-compliant, not an implementation bug. The implementer correctly flagged it as a stale-copy issue to fix in a later cross-cutting pass (affects stabddg too) rather than unilaterally rewording mid-task and risking test/contract drift.

### Strengths
- Config-building code is a verbatim, line-for-line match to the brief's literal recipe (dict literal, key order, `_apply_extra` call) — nothing invented.
- Validation isinstance-checks correctly dropped only where Pydantic now enforces the type (`list[str]`, `str`), while all three `AutobioError` messages and their trigger conditions are preserved unchanged (`src/autobio/tools/baddg.py:112-135`).
- `n_folds` now gets a host-side `ge=1, le=3` bound (`src/autobio/schemas/scoring.py:89-95`), closing the container-only range-check gap flagged in the recon fact sheet, without changing byte-compat (default 3 unaffected).
- Test suite updates are structurally sound: dropped `test_missing_mutations_raises`/`test_invalid_mutations_type_raises` for the correct reason (Pydantic now raises `ValidationError` pre-`_validate_inputs`), and adapted `test_missing_chains_raises` to pass `chains=""` since the field is now required-but-typed. `test_extra_shadowing_typed_field_rejected` and `test_info_snapshot_baddg` add genuinely new, meaningful coverage (verified `format_tool_info_catalog` really emits `x-autobio.widget` and `output_schema` via `model_json_schema()` — not a tautological check).
- Registration tests correctly pivoted to catalog assertions (`get_tool`, `CATALOG`, `TOOL_REGISTRY` absence) rather than leaving stale `TOOL_REGISTRY["baddg"]` lookups that would now KeyError.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- No single test asserts the entire `config.json` dict for equality (e.g. `assert cfg == {...}`); the 9 contract keys are covered but split across `test_basic_config` and `test_default_params`. A single full-dict assertion in one test would make byte-compat regressions (e.g., an accidental extra key) impossible to miss; current coverage catches missing/wrong values but not a stray extra top-level key introduced by a future edit.
- The stale "in the extra dict" wording in the two `AutobioError` messages (`src/autobio/tools/baddg.py:120,127`) is real end-user-facing debt now that `mutations`/`chains` are typed fields; worth a tracked follow-up (implementer already flagged this for a cross-cutting pass touching stabddg too, which is the right call rather than fixing ad hoc here).

### Assessment
**Task quality:** Approved
**Reasoning:** All four named risks check out against the actual source (byte-compat contract, `BaseInput` parentage, registry/catalog swap, and the messaging concern is correctly identified as brief-mandated rather than a defect); tests are meaningful, not rubber-stamped, and the diff matches the brief's literal code verbatim.</result>
<usage><subagent_tokens>68547</subagent_tokens><tool_uses>18</tool_uses><duration_ms>90823</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>af35813806fe62dd8</task-id>
<tool-use-id>toolu_01QTgk1zR8CZJbiV1a8br53C</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/af35813806fe62dd8.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 3: migrate stabddg" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>That directory is gitignored (scratch/report area), consistent with not needing to commit it. Everything is in order.

## Summary

**Status:** Done.

**Commit:** `89f2f3f` — `stabddg: migrate to catalog Tool with typed fields (predict mode)` (4 files: `src/autobio/schemas/scoring.py`, `src/autobio/tools/stabddg.py`, `tests/unit/test_stabddg.py`, `tests/unit/test_stabddg_e2e.py`)

**Tests:** `test_stabddg.py` + `test_stabddg_e2e.py` + `test_registry_disjoint.py` → 43 passed; full `-m "not docker and not gpu"` suite → all green; `ruff check`/`ruff format --check`/`mypy src/` all clean.

**Accurate-message wording:** empty-`mutations` now raises `AutobioError("StaB-ddG requires at least one mutation.")` (tests updated to `match="requires at least one mutation"`); the `chains` underscore-format message was left as-is (already accurate).

**Enhancements applied:** added `test_full_config_dict` asserting the entire `config.json` dict equals the expected dict for a fully-populated `StaBddGInput`.

**Concerns:** The missing-`chains` (not underscore-format) message still says "in the extra dict," matching baddg's identical pre-existing wording — left untouched since only the mutations message was flagged as misleading; worth a future joint cleanup across baddg/stabddg if desired.

**Report path:** `/home/briney/git/autobio/.superpowers/sdd/task-3-report.md` (note: `.superpowers/sdd/` is gitignored, so the report itself isn't part of the commit).</result>
<usage><subagent_tokens>115080</subagent_tokens><tool_uses>36</tool_uses><duration_ms>265882</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a5636743ce7d3908c</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/a5636743ce7d3908c.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 3 (stabddg migration)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>### Spec Compliance
- ✅ `StaBddGInput(BaseInput)` added to `src/autobio/schemas/scoring.py:29-79` — all 9 fields (structure_path, mutations, chains, mc_samples, noise_level, batch_size, trials, seed, device), types, defaults, `ge=1` constraints, widget/tier/order hints, and descriptions match the brief verbatim. `ScoringInput` (lines 14-24) is untouched — confirmed via diff (zero deletions in scoring.py, purely additive hunk).
- ✅ `config.json` byte-compat contract (`src/autobio/tools/stabddg.py:171-193`) — exact 11-key order (`pdb_path`, `mutations`, `chains`, `checkpoint_path`, `output_dir`, `mc_samples`, `noise_level`, `batch_size`, `trials`, `seed`, `device`) then `self._apply_extra(...)`, matching the brief. Independently verified via `test_full_config_dict` (`tests/unit/test_stabddg.py:445-474`), which pins the full dict with non-default values for every field and passes.
- ✅ `TOOL_REGISTRY["stabddg"]` and `_CONSUMED_EXTRA_KEYS` deleted; `STABDDG_TOOL` (catalog `Tool`/`Mode`) added with `register(STABDDG_TOOL)` at module bottom (`stabddg.py:289-331`). `_STABDDG_NOTES` reused verbatim; `_STABDDG_INPUT_FORMAT` dropped, no `input_format` on `Mode`.
- ✅ Enhancement 1 (accurate empty-mutations message): `AutobioError("StaB-ddG requires at least one mutation.")` at `stabddg.py:241-242`, with both `test_empty_mutations_raises` (`test_stabddg.py:699`) and `test_missing_mutations_fails` (`test_stabddg_e2e.py:1139`) updated to `match="requires at least one mutation"`.
- ✅ Enhancement 2 (full-dict config equality): `test_full_config_dict` present and correctly asserts all 11 keys with `==` (not per-key checks) — genuinely catches key drift, unlike per-field assertions.
- ✅ Scope respected: only `scoring.py`, `stabddg.py`, `test_stabddg.py`, `test_stabddg_e2e.py` touched (per `git diff --stat` in the diff header); no `containers/` changes.
- ✅ Verified independently (not just trusting the report): `python -m pytest tests/unit/test_stabddg.py tests/unit/test_stabddg_e2e.py tests/unit/test_registry_disjoint.py -q` → 43 passed; full `-m "not docker and not gpu"` suite → exit 0, no failures; `ruff check`/`ruff format --check` clean; `mypy src/` → no issues.

### Strengths
- `_apply_extra` collision-guard test (`test_extra_shadowing_typed_field_rejected`, `test_stabddg.py:613-637`) correctly exercises the shared base-class mechanism (`ToolRunner._apply_extra`, `src/autobio/tools/base.py:200-229`), matching the exact error substring it raises ("collide with typed input fields").
- `test_info_snapshot_stabddg` (`test_stabddg.py:825-834`) is a real, non-trivial check: it round-trips through `format_tool_info_catalog` and confirms the `x-autobio.widget == "file"` hint survives JSON-schema serialization and that `output_schema` is present — an exact mirror of the already-accepted `test_info_snapshot_baddg` pattern, not a rubber stamp.
- Runner code is structurally identical to the already-migrated `baddg.py` sibling (same import shape, same `_apply_extra` usage, same `Tool`/`Mode` construction pattern), which reduces review risk and keeps the two ddG tools consistent.
- Dropping the "must be a list of strings" host check is correctly justified: `mutations: list[str]` is now enforced by Pydantic before `_validate_inputs` ever runs.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `stabddg.py:247-251` — the missing-`chains` branch still reads `"StaB-ddG requires 'chains' in the extra dict."`, which is now inaccurate since `chains` is a typed, required field, not an extra-dict entry. This is explicitly out of scope for this task (the controller mandate only named the mutations message) and is called out by the implementer as a planned follow-up shared with `baddg.py`'s identical residue — acceptable to leave, but worth actually doing that cross-cutting cleanup soon so both messages match the migrated interface.

### Assessment
**Task quality:** Approved
**Reasoning:** All brief requirements and both controller-mandated enhancements are implemented exactly as specified, byte-compat is pinned by a genuine full-dict equality test, `ScoringInput` is untouched, and independent re-execution of tests/ruff/mypy confirms the report's claims with no discrepancies; the one residual "extra dict" wording in the chains-missing message is a pre-flagged, explicitly out-of-scope consistency nit shared with the sibling `baddg` migration.</result>
<usage><subagent_tokens>77050</subagent_tokens><tool_uses>27</tool_uses><duration_ms>183487</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ad267283bf26e3340</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/ad267283bf26e3340.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 4: migrate antipasti" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: Complete. Commit `4626911` — "antipasti: migrate to catalog Tool with typed fields (predict mode)". Test summary: 45/45 antipasti+registry unit tests pass; full suite green; mypy and ruff clean. Concerns: none — byte-compat verified via full-dict `config.json` equality assertion; note `tests/integration/test_antipasti_integration.py` (Docker-gated, out of scope) still uses the base `BindingAffinityInput`, mirroring the same pre-existing staleness left in `test_baddg_integration.py` by the sibling migrations. Report: `/home/briney/git/autobio/.superpowers/sdd/task-4-report.md`.</result>
<usage><subagent_tokens>130363</subagent_tokens><tool_uses>49</tool_uses><duration_ms>339745</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a7c8f6157daaa3892</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/a7c8f6157daaa3892.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 4 (antipasti migration)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Consistent with established sibling pattern — not a defect. I now have complete verification. Writing the final report.

### Spec Compliance
- ✅ Hints on `BindingAffinityInput` (`structure_path`/`heavy_chain`/`light_chain`/`antigen_chains`) match brief exactly — widget/tier/order per field, descriptions/types unchanged (`src/autobio/schemas/binding_affinity.py:21-39`).
- ✅ `AntipastiInput(BindingAffinityInput)` added with `modes: str | int = "all"`, correct `ui()` hint (`src/autobio/schemas/binding_affinity.py:42-49`); base `BindingAffinityInput` stays generic (no antipasti field leaked onto it).
- ✅ `config.json` contract matches the brief byte-for-byte in both keys and order: `pdb_path`, `heavy_chain`, `light_chain`, `antigen_chains`, `checkpoint_path` (`_DEFAULT_CHECKPOINT`), `output_dir`, `antipasti_dir` (`_ANTIPASTI_DIR`), `modes` (`input_data.modes`), then `self._apply_extra(config, input_data)` (`src/autobio/tools/antipasti.py:73-84`).
- ✅ Controller-mandated full-dict equality assertion present and correct: `test_full_config_byte_compat` (`tests/unit/test_antipasti.py:386-412`) asserts `cfg == {...}` with all 8 keys, both hardcoded constants spelled out literally, and `modes`.
- ✅ `modes: str | int` verified for both values: `"all"` default (`test_default_modes`) and int `100` (`test_modes_int_override`, `tests/unit/test_antipasti.py:467-485`; e2e `test_integer_modes_pipeline`, `tests/unit/test_antipasti_e2e.py:985-1005`), confirmed written verbatim to `config.json["modes"]` and flowing through the real `standardize.py` into `score_breakdown["modes"]`.
- ✅ Chain-validation `AutobioError` messages preserved verbatim: `heavy_chain`/`light_chain`/`antigen_chains`/non-empty-string/`"Duplicate chain IDs detected..."` (`src/autobio/tools/antipasti.py:114-143`, unchanged from pre-migration).
- ✅ `TOOL_REGISTRY["antipasti"]` and `_CONSUMED_EXTRA_KEYS` deleted; `_ANTIPASTI_INPUT_FORMAT` dropped; `register(ANTIPASTI_TOOL)` added; `_ANTIPASTI_NOTES` reused verbatim on the `Mode` (`src/autobio/tools/antipasti.py:166-194`).
- ✅ CPU-only contract: `requires_gpu=False`, `gpu_count=0` on `Tool`; `default_timeout=1800` on the `predict` `Mode` — verified against dataclass field placement in `core/catalog.py` (gpu fields live on `Tool`, timeout on `Mode`).
- ✅ No `containers/` changes (diff touches exactly the 4 scoped files; confirmed against `containers/antipasti/validate_config.sh` and `inference.py`, both unaffected and still compatible).
- ✅ Ran the tests myself (not just trusting the report): `pytest tests/unit/test_antipasti.py tests/unit/test_antipasti_e2e.py tests/unit/test_registry_disjoint.py` → 45 passed; full suite (`-m "not docker and not gpu"`) → 1331 passed; `ruff check` clean on all 4 touched files; `mypy` clean on the two source files.
- ✅ `test_antipasti_integration.py` left stale (still `BindingAffinityInput`, direct `runner.run()`) — confirmed this mirrors the identical pre-existing pattern in `test_baddg_integration.py` (still uses `ScoringInput` instead of `BAddGInput`), so leaving it is reasonable and consistent with established precedent, not a Task 4 defect.

### Strengths
- Byte-compat contract is exact — key order, constant values, and defaults all match the brief and the pre-migration behavior with no silent drift.
- Went beyond the minimum: added `test_extra_shadowing_typed_field_rejected` to lock in that `extra={"modes": ...}` is now correctly rejected by `_apply_extra` now that `modes` is a typed field (a real regression this migration could have silently introduced).
- The new int-`modes` e2e test exercises the full pipeline through the real `containers/antipasti/standardize.py`, not just a host-side unit assertion — good confidence that container-side `modes` handling (string vs. int) is untouched and correct.
- `_apply_extra` collision detection message match (`"collide with typed input fields"`) and `current_mode` fixture wiring both follow the exact pattern from `baddg`/`freesasa`, keeping the migration series consistent.

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `_ANTIPASTI_NOTES` still says "Key parameter (via extra dict): 'modes' ..." (`src/autobio/tools/antipasti.py:162-163`) even though `modes` is now a typed field, not an `extra`-dict key. This is now factually stale guidance surfaced to any UI/LLM consumer reading tool notes. Not a new problem introduced here — `stabddg.py:149` has the identical stale-wording pattern for its own promoted `mc_samples` field, so this is an accepted (if imperfect) precedent across the migration series rather than a defect unique to this task.

### Assessment
**Task quality:** Approved
**Reasoning:** Every element of the byte-compat contract, catalog registration, and validation-preservation requirements is verified correct against the diff and live test runs (45/45 target tests, 1331/1331 full suite, clean ruff/mypy); the one wrinkle (stale "extra dict" note wording) is a pre-existing, precedent-matching cosmetic issue, not a functional or spec defect.</result>
<usage><subagent_tokens>87616</subagent_tokens><tool_uses>34</tool_uses><duration_ms>165904</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a60e4a81431c96250</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/a60e4a81431c96250.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 5: migrate prodigy" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: Done
Commit: `af01a16` — "prodigy: migrate to catalog Tool with typed fields (predict mode)"
Tests: prodigy suites + registry-disjoint = 43 passed; full suite `python -m pytest -m "not docker and not gpu"` = 1334 passed; ruff + mypy clean.
Concerns: `tests/unit/test_tool_runner_modes.py::test_run_rejects_mode_for_legacy_tool` hardcoded `"prodigy"` as its flat-tool example and broke once prodigy migrated — fixed (in the same commit) by registering a throwaway `fakelegacytool` `TOOL_REGISTRY` entry instead, future-proofing it against later Plan 4b/4c/4d migrations. No other issues.
Report: `/home/briney/git/autobio/.superpowers/sdd/task-5-report.md`</result>
<usage><subagent_tokens>142309</subagent_tokens><tool_uses>46</tool_uses><duration_ms>320249</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a09644f42c2426067</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/a09644f42c2426067.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 5 (prodigy migration)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All confirmed. Everything in the diff matches the brief precisely, and all named risks check out clean.

### Spec Compliance
- ✅ `src/autobio/schemas/protein_binding_affinity.py:1-62` — hints added to base `ProteinBindingAffinityInput` (structure_path=FILE/PRIMARY/0, chain_selection=TEXT/PRIMARY/1, temperature=NUMBER/ADVANCED/°C/10), base stays generic (no new fields). `ProdigyInput(ProteinBindingAffinityInput)` added with `distance_cutoff: float = 5.5` (`gt=0`, NUMBER/ADVANCED/Å/step=0.1/11) and `contact_list: bool = False` (TOGGLE/ADVANCED/12) — matches brief verbatim.
- ✅ `src/autobio/tools/prodigy.py:63-70` — `config.json` byte-compat contract exact: `structure_path`, `selection` (= `chain_selection`, preserved remap, may be `None`), `temperature`, `distance_cutoff`, `contact_list`, `output_dir`, then `self._apply_extra(config, input_data)`. Verified with `test_full_config_byte_compat` (full-dict `assert cfg == {...}`) — the controller-mandated enhancement is present and correct.
- ✅ `src/autobio/tools/prodigy.py:102-114` — validation messages preserved verbatim (absolute-zero temperature message, chain_selection non-empty-or-None message).
- ✅ `src/autobio/tools/prodigy.py:139-167` — `PRODIGY_TOOL` matches brief exactly: CPU-only (`requires_gpu=False`, `gpu_count=0`), `version="2.4.0"`, `image_tag="prodigy:2.4.0"`, single `predict` mode, `default_timeout=300`, `_PRODIGY_NOTES` reused verbatim on the `Mode`, `_PRODIGY_INPUT_FORMAT` dropped, `keywords` tuple matches. `register(PRODIGY_TOOL)` present; `TOOL_REGISTRY["prodigy"]` and `_CONSUMED_EXTRA_KEYS` deleted.
- ✅ `tests/unit/test_prodigy.py`, `tests/unit/test_prodigy_e2e.py` — updated to `ProdigyInput`, typed-field top-level args, catalog registration/info-snapshot tests (`widget=="file"`, `widget=="toggle"`, `output_schema` present), extra-shadowing-rejection test added.
- ✅ No `containers/` or `tests/integration/` changes (diff confirms only the 5 listed files touched).
- ✅ Named risk (4): `tests/unit/test_tool_runner_modes.py:18-27,89-102,190-199` — `_register_fake_legacy_tool()` registers `"fakelegacytool"` into `TOOL_REGISTRY` mid-test; the `_clean_catalog` autouse fixture now also snapshots `TOOL_REGISTRY` before each test and restores it (`clear()` + `update(registry_snapshot)`) in teardown, which runs regardless of test outcome. Verified empirically: ran the 4 targeted files (53 passed) and the full suite (`python -m pytest -m "not docker and not gpu"` → 1334 passed, matching the report) — no leakage into `test_registry_disjoint.py` or elsewhere.
- Independently re-verified: `ruff check src/ tests/` clean, `ruff format --check` clean, `mypy src/` clean (56 files), full suite 1334 passed / 0 failed — all match the implementer's report.

### Strengths
- The full-dict `config.json` equality test (`test_full_config_byte_compat`) is exactly the controller-mandated enhancement and is well-placed to catch any future accidental key add/remove/rename regression in one assertion.
- `test_extra_shadowing_typed_field_rejected` correctly replaces the old `test_consumed_keys_not_leaked`, verifying `_apply_extra`'s fail-fast collision behavior now that `distance_cutoff`/`contact_list` are typed fields.
- The `test_tool_runner_modes.py` fix decouples that test from whichever tool is still un-migrated (forward-looking for Plan 4b/c/d), and the registry-snapshot/restore fix is symmetric with the existing `CATALOG` handling already in that fixture.
- Base schema (`ProteinBindingAffinityInput`) correctly kept generic — no leakage of PRODIGY-specific fields into a class that could theoretically be reused (matches recon note that this schema module is not tool-exclusive in spirit).

### Issues
#### Critical (Must Fix)
None.

#### Important (Should Fix)
None.

#### Minor (Nice to Have)
- `src/autobio/tools/prodigy.py:135-136` — `_PRODIGY_NOTES` still says "Key parameters (via extra dict): 'distance_cutoff' ... 'contact_list' ...", which is now stale (they're typed fields, not extra-dict keys). Per the task brief and controller note, this is explicitly deferred to the later uniform-cleanup task across all four Plan 4a tools — not a defect in this task.

### Assessment
**Task quality:** Approved
**Reasoning:** Every element of the brief (hints, `ProdigyInput`, byte-compat config contract with the `selection` remap, `PRODIGY_TOOL`/`register`, deletion of the flat entry, preserved validation messages) is implemented exactly as specified and independently verified against the diff and a live test/lint/type-check run; the incidental `test_tool_runner_modes.py` fix is correct and properly scoped with symmetric registry snapshot/restore, confirmed not to leak state via both the targeted suite and the full 1334-test run.</result>
<usage><subagent_tokens>80814</subagent_tokens><tool_uses>21</tool_uses><duration_ms>137078</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>aa29dfb40f865482c</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/aa29dfb40f865482c.output</output-file>
<status>completed</status>
<summary>Agent "Implement Task 6: wording polish" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: Complete.
Commit: `868557d` — "scoring/affinity: accurate typed-field wording in messages/notes + baddg full-config test"
Tests: 171 targeted tests pass; full suite `1335 passed, 121 deselected`; `mypy src/` clean; `ruff check`/`ruff format --check` clean.
Concerns: None — grep confirms zero "extra dict"/`extra['...']`/"via extra" references remain in baddg.py/stabddg.py/antipasti.py/prodigy.py; all other repo hits are out-of-scope tools. Note: `.superpowers/sdd/task-6-report.md` previously held a stale report from an unrelated earlier "Task 6" (freesasa migration); it was overwritten with this task's report as instructed (file is gitignored, not committed).
Report path: `/home/briney/git/autobio/.superpowers/sdd/task-6-report.md`</result>
<usage><subagent_tokens>86784</subagent_tokens><tool_uses>49</tool_uses><duration_ms>225173</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>ae4518126c86ac01e</task-id>
<tool-use-id>toolu_011Z7giiktBmcaTZdS3BXMmH</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/ae4518126c86ac01e.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 6 (wording polish)" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Spec Compliance

✅ [src/autobio/tools/baddg.py:64] Empty-mutations message reworded
✅ [src/autobio/tools/baddg.py:72] Empty/missing-chains message reworded  
✅ [src/autobio/tools/baddg.py:95] _BADDG_NOTES: "extra['n_folds']" → "n_folds"
✅ [src/autobio/tools/stabddg.py:152] Empty/missing-chains message reworded
✅ [src/autobio/tools/stabddg.py:174-179] _STABDDG_NOTES: dropped "(via extra dict):" and quotes from field names
✅ [src/autobio/tools/antipasti.py:153] _ANTIPASTI_NOTES: dropped "(via extra dict):" and quotes from 'modes'
✅ [src/autobio/tools/prodigy.py:121-122] _PRODIGY_NOTES: dropped "(via extra dict):" and quotes from field names
✅ [tests/unit/test_baddg.py:205-229] New test_full_config_byte_compat: full 9-key dict equality with mutations comma-joined
✅ [tests/unit/test_baddg.py:253] test_empty_mutations_raises: match regex updated
✅ [tests/unit/test_baddg.py:267] test_missing_chains_raises: match regex updated
✅ [tests/unit/test_baddg_e2e.py:294] test_missing_mutations_fails: match regex updated
✅ [tests/unit/test_stabddg.py:321] test_missing_chains_raises: match regex updated

## Strengths

- All reworded `AutobioError` messages match their test regexes exactly (verified substring matches)
- All four `_*_NOTES` strings correctly drop the "via extra dict" / `extra['...']` framing while preserving defaults and descriptions verbatim
- New baddg byte-compat test correctly asserts the exact 9-key config contract: pdb_path, mutations (comma-joined as "YH103H,QD30V"), chains, mpnn_checkpoint_path, ddg_checkpoint_path, output_dir, n_folds, seed, device
- No changes to config dict construction, field defaults, or runtime logic — only strings and one additive test
- No containers/ or tests/integration/ modifications
- Test suite ran to completion (171 unit tests passed on the four tools, 1335 total with other tests)

## Issues

None found. All changes are string-only (error messages and notes) plus one test assertion. The diff contains no logic changes, no field defaults, no config dict modifications, and all reworded messages have corresponding test regex updates.

## Assessment

**Task quality:** Approved

**Reasoning:** Implementation fully satisfies the spec: all `AutobioError` messages and `_*_NOTES` strings have been accurately reworded to drop extra-dict framing for now-typed fields, test regexes are updated, and the new byte-compat test for baddg pins the exact 9-key contract. No behavioral or schema changes were made.</result>
<usage><subagent_tokens>27891</subagent_tokens><tool_uses>2</tool_uses><duration_ms>30619</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a9c2f923628d76b12</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/a9c2f923628d76b12.output</output-file>
<status>completed</status>
<summary>Agent "Final whole-branch review Plan 4a" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have completed a thorough review. I traced every config.json contract by hand against the pre-migration code, verified the shared-schema invariants, and confirmed the deferred integration-test breakage. Here is my assessment.

---

## Code Review: Tools→Modes Plan 4a — SCORING singleton migrations

### Strengths

- **Byte-compat is genuinely preserved.** I traced each of the four `prepare_workspace` config dicts against the pre-migration `extra.get(...)` code path key-by-key, value-by-value, and in insertion order:
  - `baddg`/`stabddg`: `mutations` comma-join (`",".join(input_data.mutations)`), `chains` passthrough, checkpoint constants, `output_dir`, and all numeric/string params map identically. Defaults (`n_folds=3`, `mc_samples=20`, `batch_size=10000`, `trials=1`, `seed=0`, `noise_level=0.1`, `device="auto"`) match the old `.get(...)` fallbacks.
  - `antipasti`: `modes` default `"all"`, plus `checkpoint_path`/`antipasti_dir` constants — identical.
  - `prodigy`: the `chain_selection`→`"selection"` remap is preserved (`src/autobio/tools/prodigy.py:~40`), `distance_cutoff=5.5`/`contact_list=False` defaults match. The full-dict test at `tests/unit/test_prodigy.py` (`test_full_config_byte_compat`) explicitly locks the remap.
  - All four ship exact full-dict equality tests (baddg/stabddg use non-default values, which is the stronger form).
- **`_apply_extra` strengthening is correctly wired.** The collision check runs against `config` *after* all derived keys are inserted (it is the last statement in every `prepare_workspace`), `run()` sets `current_mode` before `prepare_workspace` (`src/autobio/tools/base.py:129` → `:136`), and `BaseInput.extra` always defaults to a dict (`src/autobio/schemas/base.py:30`), so the `for key in input_data.extra` scan is safe. The second clause (`key in config`) genuinely closes a real footgun that existed for `pdb_path`/`checkpoint_path`/`output_dir` (non-typed derived keys the old flat-merge would have silently overwritten). No legitimate workflow is broken — all now-blocked keys are container-internal paths.
- **`ScoringInput` is genuinely untouched** (no diff lines; only additive `BAddGInput`/`StaBddGInput` appended) and remains consumed by openmm, ligandmpnn_packer, evoef2, antifold, esm_if1, rosetta.
- **New input classes are correctly scoped.** `BAddGInput`/`StaBddGInput` subclass `BaseInput`; `AntipastiInput`/`ProdigyInput` subclass the affinity bases. Tool-specific fields (`modes`, `distance_cutoff`, `contact_list`) are added on the *subclasses*, not the shared bases. The shared bases (`BindingAffinityInput`, `ProteinBindingAffinityInput`) have no consumers other than their one tool each plus schema exports, so the added `x-autobio` hints on base fields are safe and non-serializing.
- **Flat entries fully removed** (no `TOOL_REGISTRY[...]`/`ToolEntry`/`_CONSUMED_EXTRA_KEYS`/`input_format`/`supports_batch` left in the four modules), still present in `TOOL_RUNNERS`, disjointness guard intact.
- **Consistent migration** across all four (single `predict` mode, `default_mode="predict"`, keyword tuples, display names, `&lt;NAME&gt;_TOOL` constant + docstring + `register(...)`).
- **Good test future-proofing:** `test_run_rejects_mode_for_legacy_tool` no longer depends on `prodigy` staying legacy — it registers a `fakelegacytool` and the autouse fixture now snapshots/restores `TOOL_REGISTRY` symmetrically with `CATALOG`. Validation messages were corrected to drop stale "extra dict" wording, and the matching tests were updated in lockstep.

### Issues

#### Critical (Must Fix)
None.

#### Important (Should Fix)
- **Stale integration tests are guaranteed-broken, not merely un-run.** `tests/integration/test_baddg_integration.py:66`, `:108`, `tests/integration/test_stabddg_integration.py:68`,`:114`, `tests/integration/test_antipasti_integration.py:74`, `tests/integration/test_prodigy_integration.py:76`,`:116` still construct the old base classes (`ScoringInput(... extra={"mutations":..., "chains":...})`, `BindingAffinityInput(...)`, `ProteinBindingAffinityInput(...)`) and call `runner.run(...)`. Every runner now begins with `assert isinstance(input_data, BAddGInput/StaBddGInput/AntipastiInput/ProdigyInput)` — a `ScoringInput` is not a `BAddGInput`, so these will raise `AssertionError` (and then `AttributeError` on `input_data.mutations`) the moment the Docker suite runs. **Why it matters:** spec §10 permits *deferring the run*, but the fix requires no Docker — it is the identical mechanical transform already applied to the `*_e2e.py` files (swap the input class, hoist `extra` keys to top-level fields). Leaving them is a latent failure that will look like a regression to the next person who runs `pytest -m docker`. **How to fix:** apply the same input-class swap in the four integration files (or, at minimum, file a tracked follow-up issue and note it in the PR description). I lean toward doing it in this PR since it is Docker-free to write.

#### Minor (Nice to Have)
- **`AntipastiInput.modes` field name shadows the Tool-level `modes` concept.** `src/autobio/schemas/binding_affinity.py` adds a field literally named `modes` to a `Tool` whose mode dict is also `.modes`. No functional collision (different objects/namespaces; `_apply_extra` treats it correctly as a typed field), and it matches the container's config key, so changing it would break byte-compat — but it is a readability trap worth a code comment.
- **New numeric bounds are a (benign) behavior tightening.** `n_folds` gains `ge=1, le=3`; `mc_samples`/`batch_size`/`trials` gain `ge=1`; `distance_cutoff` gains `gt=0`. The old `extra.get(...)` path passed any value through to the container. This is per the plan's contract tables and aligns with the fail-fast guidance, and all defaults are in-range so there is no byte-compat regression — but out-of-range values that previously reached the container now raise a Pydantic `ValidationError` at construction. Intentional and positive; just flagging the semantics change.
- **Registration test asymmetry:** `baddg` and `stabddg` registration classes lack the `test_timeout` assertion that `antipasti`/`prodigy` have; their `default_timeout=600` is only covered indirectly. Cheap to add for parity.
- **`antipasti` full-dict test uses only defaults** (`modes="all"`); the non-default integer path is covered separately by `test_modes_int_override`, so coverage is adequate, but a single full-dict assertion with a non-default `modes` would be marginally stronger (matching the baddg/stabddg pattern).

### Recommendations
1. Update the four `tests/integration/test_*_integration.py` files with the same input-class swap used in the `*_e2e.py` tests (Docker-free edit), or file and reference a tracking issue in the PR.
2. Add a one-line comment on `AntipastiInput.modes` clarifying it is the DCCM normal-mode count, distinct from `Tool.modes`.
3. (Optional) add `test_timeout` to the baddg/stabddg registration tests for parity.

### Assessment
**Ready to merge?** With fixes

**Reasoning:** The production code is correct and complete — byte-compat is preserved for all four tools (I verified each contract by hand, including prodigy's `selection` remap and the comma-joins), `_apply_extra` is soundly strengthened and wired, `ScoringInput` is untouched, and the new schemas are cleanly scoped. The only substantive gap is the four stale Docker-gated integration tests, which are guaranteed-broken and fixable without Docker; update them (or explicitly track the deferral) before merge.</result>
<usage><subagent_tokens>145844</subagent_tokens><tool_uses>11</tool_uses><duration_ms>300106</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>adeebc433ae5b0ba8</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/tmp/claude-1001/-home-briney-git-autobio/bd522b89-efe2-4c84-9179-69456ba55dac/tasks/adeebc433ae5b0ba8.output</output-file>
<status>completed</status>
<summary>Agent "Fix final-review findings" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: Complete.

Commit: `9523993` — "test: update Plan 4a integration tests to typed inputs; modes comment; timeout parity"

Verification summary: integration test files rewritten to construct `BAddGInput`/`StaBddGInput`/`AntipastiInput`/`ProdigyInput` (no `extra={}`); collect-only succeeds, standalone construction snippet confirms zero Pydantic errors, grep confirms no stale old-class/`extra={` usage remains; `test_timeout` added to baddg/stabddg registration tests (600); `AntipastiInput.modes` description clarified. Full suite `1337 passed, 121 deselected`; `mypy src/` clean; `ruff check`/`format` clean.

Concerns: none — `containers/` and `src/autobio/tools/` untouched; only integration tests, the schema docstring, and the two unit-test timeout assertions changed.

Report path: `/home/briney/git/autobio/.superpowers/sdd/task-6-report.md` (appended new "Follow-up: whole-branch final-review fixes" section; file is gitignored, not committed).</result>
<usage><subagent_tokens>112620</subagent_tokens><tool_uses>61</tool_uses><duration_ms>239446</duration_ms></usage>
</task-notification>